# grule-lint
grl-lint is a comprehensive linting and validation tool for Grule Rule Language (GRL) files. It helps developers catch syntax errors, validate rule logic, and ensure compliance with domain-specific constraints before runtime. Built with performance in mind, it supports parallel file processing, custom validation rules.
